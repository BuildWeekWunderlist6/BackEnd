const db = require('../data/db');

const getByList = todoListId => {

};

const getById = id => {

};

const create = (todoListId, todoItem) => {

};

const update = (id, updates) => {

};

const remove = id => {

};

module.exports = {
    getByList,
    getById,
    create,
    update,
    remove
};
